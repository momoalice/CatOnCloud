<%= form_with(model: user_auth, local: true) do |form| %>
  <% if user_auth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_auth.errors.count, "error") %> prohibited this user_auth from being saved:</h2>

      <ul>
      <% user_auth.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_auth_name %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password, id: :user_auth_password %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :user_auth_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
